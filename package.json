{
  "name": "climate-lens",
  "version": "1.0.0",
  "description": "AI-powered carbon footprint tracker and waste recycling finder",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx -y concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "npx -y concurrently \"npm --prefix backend start\" \"npm --prefix frontend run dev\""
  }
}
